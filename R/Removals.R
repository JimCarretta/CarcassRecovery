# Store published estimates of human-related removals from literature

Species <- c("Harbor Porpoise", "Common Dolphin (all spp.)", "Humpback Whale")

Year <- seq(1990, 2017, 1)

removalsPP <- c(62)
  CV.removalsPP <- c(0.44)